import React from 'react';
import { Loadable, RecoilValueReadOnly } from 'recoil';
import { Card } from '@material-ui/core';
import { List } from 'immutable';

type Props ={
    height: number;
    width: number;
    selectors: List<RecoilValueReadOnly<any>>;
}
export function FineedContainer(props: Props){
    
    
}